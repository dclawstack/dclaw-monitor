apiVersion: v1
kind: Secret
metadata:
  name: dclaw-monitor-secret
  namespace: dclaw-core
  labels:
    app.kubernetes.io/name: dclaw-monitor
    app.kubernetes.io/part-of: dclaw-platform
type: Opaque
stringData:
  DATABASE_URL: "postgresql+asyncpg://postgres:postgres@dclaw-shared-postgres:5432/dclaw_monitor"
  APP_ENV: "production"
  SECRET_KEY: "dclaw-monitor-local-secret-key"
