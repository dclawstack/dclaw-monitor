apiVersion: apps/v1
kind: Deployment
metadata:
  name: dclaw-monitor-frontend
  namespace: dclaw-core
  labels:
    app.kubernetes.io/name: dclaw-monitor
    app.kubernetes.io/part-of: dclaw-platform
    app.kubernetes.io/component: frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: dclaw-monitor
  template:
    metadata:
      labels:
        app.kubernetes.io/name: dclaw-monitor
        app.kubernetes.io/part-of: dclaw-platform
        app.kubernetes.io/component: frontend
    spec:
      containers:
        - name: frontend
          image: docker.io/library/dclaw-monitor-frontend:local
          imagePullPolicy: Never
          ports:
            - containerPort: 3030
              name: http
          readinessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            requests:
              cpu: "50m"
              memory: "128Mi"
            limits:
              cpu: "200m"
              memory: "256Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: dclaw-monitor
  namespace: dclaw-core
  labels:
    app.kubernetes.io/name: dclaw-monitor
    app.kubernetes.io/part-of: dclaw-platform
    app.kubernetes.io/component: frontend
spec:
  type: NodePort
  ports:
    - port: 3030
      targetPort: http
      nodePort: 30030
      name: http
  selector:
    app.kubernetes.io/name: dclaw-monitor
