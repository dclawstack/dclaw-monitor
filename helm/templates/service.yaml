apiVersion: v1
kind: Service
metadata:
  name: {{ include "dclaw-monitor.fullname" . }}-backend
  labels:
    app: {{ include "dclaw-monitor.name" . }}
    component: backend
spec:
  type: {{ .Values.service.backend.type }}
  ports:
    - port: {{ .Values.service.backend.port }}
      targetPort: 8030
      protocol: TCP
      name: http
  selector:
    app: {{ include "dclaw-monitor.name" . }}
    component: backend

---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "dclaw-monitor.fullname" . }}-frontend
  labels:
    app: {{ include "dclaw-monitor.name" . }}
    component: frontend
spec:
  type: {{ .Values.service.frontend.type }}
  ports:
    - port: {{ .Values.service.frontend.port }}
      targetPort: 3030
      protocol: TCP
      name: http
  selector:
    app: {{ include "dclaw-monitor.name" . }}
    component: frontend
