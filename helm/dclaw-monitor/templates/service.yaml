apiVersion: v1
kind: Service
metadata:
  name: {{ include "dclaw-monitor.fullname" . }}
  labels:
    {{- include "dclaw-monitor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dclaw-monitor.selectorLabels" . | nindent 4 }}
